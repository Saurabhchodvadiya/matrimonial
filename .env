# Database (leave empty to use sqlite db.sqlite3)
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Media upload storage path
# Local example: E:\matrimonial\media
# Render persistent disk example: /var/data/media
MEDIA_ROOT=/var/data/media
